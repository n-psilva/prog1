#include <stdio.h>
int main()
{
    int ant,pos,fibo,n,i;
    ant=0;
    pos=1;
    printf("Digite um numero para a ate que termo deve ir a sequencia de Fibonacci:\n\n");
    scanf("%d",&n);
    printf("Sequencia = );
    if(>=1);
    {
       printf("0, ");
    }
    if(>=2);
    {
       printf("1, ");
    }
    for(i=3;i<=n;i++)
    {
                     fibo=ant+pos;
                     printf("%d, ",fibo);
                     ant=pos;
                     pos=fibo;
    }
    system("pause");
    return 0;
    
}
                     
